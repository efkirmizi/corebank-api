# Development and test dependencies.
-r requirements.txt
pytest>=8,<9
pytest-cov>=5,<7
requests>=2.31,<3

# Linters are pinned narrowly on purpose: formatting and lint rules change
# between releases, and an unpinned bump would fail CI on untouched code.
ruff~=0.15.22
black~=26.5.1
